# https://open.kattis.com/problems/ones

import sys

for line in sys.stdin:
    number = int(line)
    k = 0
    a = False
    retenue = -1
    L = [number % 10, (number//10)%10, (number//100)%10,(number//1000)%10]
    while retenue != 0:
        if retenue == -1:
            retenue = 0
        if k == 0:
            n = L[0]*
    
        
        

        
